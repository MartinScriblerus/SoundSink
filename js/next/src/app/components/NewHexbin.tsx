import React, { useEffect, useRef, useCallback } from 'react';
import * as BABYLON from 'babylonjs';

type NewHexProps = {
    game: any;
    numHexes: number;
    values: any;
    onHexClick: (hexValue: string) => void;
}

const FullScreenHexbin = ({ game, numHexes, values, onHexClick }: NewHexProps) => {
    


      return (
        // <canvas
        //   ref={game.canvas2}
        //   style={{
        //     width: "100vw",
        //     height: "100vh",
        //   }}
        // />
        <></>
      );
};
    


export default FullScreenHexbin;