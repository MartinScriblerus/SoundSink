"use client"

import React, { useEffect, useMemo, useState } from 'react';

const FrameComponent = ({ children, width, height, backgroundColor, borderRadius, borderWidth, borderColor, boxShadow }: any) => {
    
    return (<>
    
    </>)
}

export default FrameComponent;
